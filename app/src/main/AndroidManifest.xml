<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:forceDarkAllowed="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD_POP">

        <activity android:name=".ui.ChatActivity" />
        <activity android:name=".ui.PaymentConfirmationActivity" />
        <activity android:name=".ui.PaymentActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.CourseDetailActivity" />
        <activity android:name=".ui.MentorDataVaultActivity" />
        <activity android:name=".ui.MentorListActivity" />
        <activity android:name=".ui.MentorChatsActivity" />
        <activity android:name=".ui.CreateCourseActivity" />
        <activity android:name=".ui.MentorCoursesActivity" />
        <activity android:name=".ui.MenteeDashboardActivity" />
        <activity android:name=".ui.MentorDashboardActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity
            android:name=".ui.LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.MAD_POP.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />
    </application>

</manifest>